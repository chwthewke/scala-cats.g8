server {
  listen-address = "127.0.0.1"
  listen-port = $httpPort$
  base-uri = "http://localhost:$httpPort$/"
}

logging {
  log-request = true
  log-request-body = false
  log-response = true
  log-response-body = false
}
